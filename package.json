{
  "name": "parallaxer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.local.js",
    "build:local": "vite build --config vite.config.local.js",
    "build:pages": "vite build --config vite.config.pages.js",
    "preview": "vite preview --config vite.config.local.js"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.6",
    "@ffmpeg/ffmpeg": "^0.12.10",
    "@ffmpeg/util": "^0.12.1",
    "onnxruntime-web": "^1.22.0"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
